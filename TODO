- Load config from file from configurable path
 - Loading done, not configurable yet
- Handle interrupt to resync (load/unload) breakpoints from config file
- Refactor parser class to be tidier
- Ensure tests teardown as well, so:
 - the prepending doesn't bleed between tests
 - we can reuse test files
 - tearing down is thoroughly tested
- Integration tests
- Refactor test suite
- More tests
- More test files to inject
- Test injecting on modules
- Handle single-line function definitions by parsing columns
- Improve docs, move to bpf.sh report
